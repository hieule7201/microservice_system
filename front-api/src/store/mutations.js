export function setAllBook(state, books) {
   state.allBooks = books || []
}
export function setMessageAPI(state, message) {
   state.messageAPI = message || []
}
export function setDetailsBook(state, book) {
   state.bookDetail = book || []
}
export function setAllBorrow(state, books) {
   state.borrows = books || []
}

